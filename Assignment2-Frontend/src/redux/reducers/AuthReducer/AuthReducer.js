/* eslint-disable no-param-reassign */

const initialState = {
  user: {},
};

export function authReducer(state = initialState, action) {
  switch (action.type) {
   

    default: {
      return state;
    }
  }
}
