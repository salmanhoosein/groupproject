import * as actions from '../../actions/HistoryAction/HistoryAction';

const initialState = {
 
};

export function historyReducer(state = initialState, action) {
  switch (action.type) {
    
    default:
      return state;
  }
}
